run:
  timeout: 5m

linters-settings:
  govet:
    enable-all: true
  gocyclo:
    min-complexity: 15

linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused